{{- $computed := dict }}
{{- $_ := set $computed "kubeProxyReplacement" .Values.kubeProxyReplacement }}
{{- $_ := set $computed "k8sServiceHost" .Values.k8s.serviceHost }}
{{- $_ := set $computed "k8sServicePort" (int .Values.k8s.servicePort) }}
{{- $_ := set $computed "operator"        (dict "replicas" (int .Values.operatorReplicas)) }}
{{- $_ := set $computed "gatewayAPI"      (dict "enabled" .Values.gatewayAPI.enabled) }}
{{- $_ := set $computed "l2announcements" (dict "enabled" .Values.l2announcements.enabled) }}
{{- $_ := set $computed "externalIPs"     (dict "enabled" .Values.externalIPs.enabled) }}

{{- $extra := .Values.extraValues | default dict }}
{{- $merged := mergeOverwrite $computed (deepCopy $extra) }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cilium
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
spec:
  project: {{ .Values.project | quote }}
  source:
    repoURL: https://helm.cilium.io
    chart: cilium
    targetRevision: {{ .Values.chartVersion | quote }}
    helm:
      releaseName: cilium
      valuesObject:
        {{- toYaml $merged | nindent 8 }}
  destination:
    server: {{ .Values.destination.server | quote }}
    namespace: {{ .Values.destination.namespace | quote }}
  syncPolicy:
    {{- toYaml .Values.syncPolicy | nindent 4 }}

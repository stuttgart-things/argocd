{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "cert-manager-vault-pki chart",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "vault"
  ],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1
    },
    "vault": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "server",
        "path",
        "auth"
      ],
      "properties": {
        "server": {
          "type": "string",
          "format": "uri",
          "minLength": 1
        },
        "path": {
          "type": "string",
          "minLength": 1
        },
        "caBundleSecretRef": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "name",
            "key"
          ],
          "description": "Optional Secret in the cert-manager namespace carrying the Vault server's CA bundle (PEM under the configured key). cert-manager uses it to verify the Vault server's TLS. Typically `{ name: vault-pki-ca, key: ca.crt }` — the Secret produced by argocd.create-vault-issuer or by the AppSet itself.",
          "properties": {
            "name": {
              "type": "string",
              "minLength": 1
            },
            "key": {
              "type": "string",
              "minLength": 1
            }
          }
        },
        "auth": {
          "type": "object",
          "additionalProperties": false,
          "description": "How the ClusterIssuer authenticates to Vault. With method \"token\" a static Secret-backed token is used, which nothing renews and which Vault caps at 768h — prefer \"kubernetes\", where cert-manager mints a short-lived ServiceAccount token per request. Both sub-objects may be present; only the one matching `method` is rendered, which lets the ApplicationSet pass both unconditionally (bare-line conditionals in helm.valuesObject break kustomize build).",
          "properties": {
            "method": {
              "type": "string",
              "enum": [
                "",
                "token",
                "kubernetes"
              ],
              "default": "token",
              "description": "Defaults to \"token\" when absent or empty, so existing callers are unaffected. The empty string is allowed because ApplicationSet templating renders a missing cluster annotation as \"\"."
            },
            "tokenSecretRef": {
              "type": "object",
              "additionalProperties": false,
              "description": "Used when method is \"token\". Values may be empty strings when method is \"kubernetes\".",
              "properties": {
                "name": {
                  "type": "string"
                },
                "key": {
                  "type": "string"
                }
              }
            },
            "kubernetes": {
              "type": "object",
              "additionalProperties": false,
              "description": "Used when method is \"kubernetes\". Values may be empty strings when method is \"token\".",
              "properties": {
                "mountPath": {
                  "type": "string",
                  "description": "Vault Kubernetes auth mount including the /v1/auth/ prefix, e.g. \"/v1/auth/my-cluster-certmanager\"."
                },
                "role": {
                  "type": "string",
                  "description": "Vault role name on that mount."
                },
                "serviceAccountRef": {
                  "type": "object",
                  "additionalProperties": false,
                  "description": "ServiceAccount cert-manager presents when logging in. Defaults to the role name when empty.",
                  "properties": {
                    "name": {
                      "type": "string"
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "rbac": {
      "type": "object",
      "additionalProperties": false,
      "description": "RBAC letting the cert-manager controller mint the Vault login token via the TokenRequest API. Rendered only when vault.auth.method is \"kubernetes\". The cert-manager chart shipped this Role up to v1.18.x and stopped in v1.21.x, with no values flag to restore it — and its absence is silent: the ClusterIssuer still reports Ready=True (cert-manager verifies the Vault login, never the ability to sign) while no certificate is ever issued.",
      "properties": {
        "create": {
          "type": "boolean",
          "default": true,
          "description": "Ship the cert-manager-tokenrequest Role/RoleBinding. Set false only where something else already owns that Role in this namespace — e.g. a cluster whose issuer comes from stuttgart-things/flux, which ships the same pair."
        },
        "tokenRequesterServiceAccount": {
          "type": "string",
          "minLength": 1,
          "default": "cert-manager",
          "description": "ServiceAccount that CALLS TokenRequest — the cert-manager controller, not the ServiceAccount whose token is minted (that is vault.auth.kubernetes.serviceAccountRef.name, which the Role's resourceNames follows)."
        }
      }
    },
    "destination": {
      "type": "object",
      "additionalProperties": true,
      "description": "Inert pass-through. Present so verify-catalog's probe values do not fail strict additionalProperties:false validation; the chart itself does not consume this field."
    }
  }
}

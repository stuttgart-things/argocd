---
apiVersion: v2
name: external-secrets-cluster-secret-store-vault
description: "`ClusterSecretStore`s for HashiCorp Vault (k8s-auth, KV v2) — one per KV mount in `mounts`, or a single one from `name`+`path`. Cluster-specific: `auth.kubernetes.mountPath` must match the Vault k8s auth backend mounted for the target cluster."
type: application
version: 0.2.0

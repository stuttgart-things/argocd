{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "machinery-configmap chart",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "content"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "ConfigMap name. Must match the volume name the Deployment patch in apps/machinery/install mounts."
    },
    "content": {
      "type": "string",
      "minLength": 1,
      "description": "Full machinery config.json content as a string."
    }
  }
}

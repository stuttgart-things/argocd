{{- $defaultName := printf "machinery-%s" (sha1sum .Values.destination.server | trunc 8) }}
{{- $appName := .Values.applicationName | default $defaultName }}
{{- $deployPatch := dict
      "apiVersion" "apps/v1"
      "kind" "Deployment"
      "metadata" (dict "name" "machinery")
      "spec" (dict "template" (dict "spec" (dict
        "containers" (list
          (dict
            "name" "machinery"
            "image" (printf "%s:%s" .Values.image.repository .Values.image.tag)
            "env" (list (dict "name" "MACHINERY_CONFIG" "value" "/etc/machinery/config.json"))
            "ports" (list
              (dict "name" "grpc" "containerPort" 50051 "protocol" "TCP")
              (dict "name" "http" "containerPort" 8080  "protocol" "TCP")
            )
            "volumeMounts" (list (dict
              "name"      "machinery-config"
              "mountPath" "/etc/machinery"
              "readOnly"  true
            ))
          )
        )
        "volumes" (list (dict
          "name" "machinery-config"
          "configMap" (dict "name" "machinery-config" "optional" true)
        ))
      )))
}}
{{- $svcPatch := dict
      "apiVersion" "v1"
      "kind" "Service"
      "metadata" (dict "name" "machinery")
      "spec" (dict "ports" (list
        (dict "name" "grpc" "port" 50051 "targetPort" "grpc" "protocol" "TCP")
        (dict "name" "http" "port" 8080  "targetPort" "http" "protocol" "TCP")
      ))
}}
{{- $hrDelete := dict
      "apiVersion" "gateway.networking.k8s.io/v1"
      "kind" "HTTPRoute"
      "metadata" (dict "name" "machinery")
      "$patch" "delete"
}}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $appName | quote }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: {{ .Values.project | quote }}
  source:
    repoURL: {{ .Values.kustomize.repoURL | quote }}
    targetRevision: {{ .Values.kustomize.targetRevision | quote }}
    path: .
    kustomize:
      patches:
        - target:
            kind: Deployment
            name: machinery
          patch: |
{{ toYaml $deployPatch | indent 12 }}
        - target:
            kind: Service
            name: machinery
          patch: |
{{ toYaml $svcPatch | indent 12 }}
        {{- if .Values.httpRoute.enabled }}
        - target:
            kind: HTTPRoute
            name: machinery
          patch: |
{{ toYaml $hrDelete | indent 12 }}
        {{- end }}
  destination:
    server: {{ .Values.destination.server | quote }}
    namespace: {{ .Values.destination.namespace | quote }}
  syncPolicy:
    {{- toYaml .Values.syncPolicy | nindent 4 }}

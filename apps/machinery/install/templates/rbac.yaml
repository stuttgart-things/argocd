{{/* Effective rules = explicit `rbac.rules` UNION the rules derived from
`config.watch` (one per apiGroup, see _helpers.tpl). Deriving from the
same catalog the watch config is built from keeps RBAC and the watch set
from ever drifting — add a kind to `watch` and its list/watch permission
comes along automatically. */}}
{{- $explicitRules := list }}
{{- if and .Values.rbac .Values.rbac.rules }}{{- $explicitRules = .Values.rbac.rules }}{{- end }}
{{- $watchRules := include "machinery.watchRbacRules" . | fromJsonArray }}
{{- $allRules := concat $explicitRules $watchRules }}
{{- if $allRules }}
{{- $defaultName := printf "machinery-%s" (sha1sum .Values.destination.server | trunc 8) }}
{{- $appName := .Values.applicationName | default $defaultName }}
{{- $roleName := default (printf "%s-reader" $appName) .Values.rbac.name }}
---
# ClusterRole + binding for the machinery ServiceAccount. The KCL base
# in machinery-kustomize ships only the ServiceAccount — without this
# Application, machinery's list/watch calls for anything outside the
# `view` aggregation come back forbidden, and the dashboard just
# error-loops. Cluster-scoped so a single binding covers whichever
# kinds `config.watch` selects (rules derived from the catalog) plus any
# extra `rbac.rules`.
#
# Sync-wave 0: same as the kustomize Application so the binding lands
# in the same pass that creates the SA.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "%s-rbac" $appName | quote }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  # See chart.yaml — without the finalizer the ClusterRole + Binding on
  # the destination cluster are orphaned when the parent App is deleted.
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.project | quote }}
  source:
    repoURL: {{ .Values.catalog.repoURL | quote }}
    targetRevision: {{ .Values.catalog.targetRevision | quote }}
    path: apps/machinery/rbac
    helm:
      releaseName: machinery-rbac
      valuesObject:
        name: {{ $roleName | quote }}
        serviceAccount:
          name: machinery
          namespace: {{ .Values.destination.namespace | quote }}
        rules:
          {{- toYaml $allRules | nindent 10 }}
  destination:
    # ClusterRole + ClusterRoleBinding are cluster-scoped; namespace is
    # ignored by the API server but Argo wants something here.
    server: {{ .Values.destination.server | quote }}
    namespace: {{ .Values.destination.namespace | quote }}
  syncPolicy:
    {{- toYaml .Values.syncPolicy | nindent 4 }}
{{- end }}

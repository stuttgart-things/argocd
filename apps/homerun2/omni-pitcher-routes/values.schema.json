{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "homerun2-omni-pitcher-routes chart",
  "type": "object",
  "_comment": "additionalProperties left open so the catalog verifier (call-argocd-verify-catalog) can inject its common probe values like `destination` without tripping the schema. The chart only consumes `name` and `content`; anything else is ignored.",
  "additionalProperties": true,
  "required": ["name", "content"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "ConfigMap name. Must match omniPitcherRoutesConfigMap in apps/homerun2/install, which the Deployment patch mounts."
    },
    "content": {
      "type": "string",
      "minLength": 1,
      "description": "The routing file as a YAML string: streams allowlist, default_stream, and ordered routes."
    }
  }
}

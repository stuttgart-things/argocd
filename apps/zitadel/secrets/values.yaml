---
namespace: zitadel
secretName: zitadel-masterkey       # pragma: allowlist secret
values:
  # ZITADEL masterkey — must be exactly 32 characters.
  masterkey: ""                     # pragma: allowlist secret

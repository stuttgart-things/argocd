---
# Crossplane Configuration packages to install.
#
# Each entry:
#   name:    Configuration resource name
#   package: full xpkg reference including tag
#
# The default list is intentionally empty while the Configurations are
# migrated from the old ghcr.io/stuttgart-things/crossplane/... packages
# to ghcr.io/stuttgart-things/crossplane-configurations/... — entries
# will be re-added one at a time as their replacements are pushed and
# verified.
configurations: []
